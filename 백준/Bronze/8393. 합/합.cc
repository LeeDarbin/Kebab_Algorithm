#include <iostream>
#include <stdio.h>
#include <algorithm>
using namespace std;

int a, b, c;
int main(){
    cin.tie(nullptr);
    ios_base::sync_with_stdio(0);

    cin >> c;
    cout << (1 + c)*c/2;
}